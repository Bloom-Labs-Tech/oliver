{
  "name": "oliver",
  "private": true,
  "scripts": {
    "clean": "rm -f bun.lockb",
    "start": "concurrently \"cd packages/bot && bun start\" \"cd packages/dashboard && bun start\"",
    "dev": "concurrently \"cd packages/bot && bun dev\" \"cd packages/dashboard && bun dev\""
  },
  "workspaces": [
    "apps/*",
    "lib/*",
    "packages/*"
  ]
}
